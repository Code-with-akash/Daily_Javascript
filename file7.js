// Most Common Methods of strings

// Trim
// toUppercase
// toLowercase
// slice()

let firstName = "     Akash";

length_of_fisrtName = firstName.length
console.log(length_of_fisrtName) // here the length is 10 

// need to trim some space or any specail char

firstName = firstName.trim()
console.log(firstName) // it will print the string
console.log(firstName.length) // After triming the length is 5

// if we want to convert the string to uppercase

console.log(firstName.toUpperCase())

// if we want to convert the string to lowercase

console.log(firstName.toLowerCase())


// slice is used to - slice the part of a string .

console.log(firstName.slice(0,2)) 


// by codewithakash